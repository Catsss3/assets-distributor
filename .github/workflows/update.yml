
name: Auto Update
on:
  schedule: [{cron: "0 */6 * * *"}]
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install requests
      - run: python core_sync.py
      - name: Check files and push
        run: |
          git config user.name "Blondie-Bot"
          git config user.email "bot@ai.com"
          # Если файлы появились, добавляем и пушим
          if ls sub*.txt 1> /dev/null 2>&1; then
            git add sub*.txt
            git commit -m "🚀 Auto-Update"
            git push
          else
            print "Никаких изменений не найдено."
          fi
