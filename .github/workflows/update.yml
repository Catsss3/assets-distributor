
name: Auto Update
on:
  schedule: [{cron: "0 */6 * * *"}]
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install requests google-generativeai
      - run: python core_sync.py
      - run: |
          git config user.name "Blondie-Bot"
          git config user.email "bot@ai.com"
          git add .
          git commit -m "🚀 Auto-Update (Full Sources + Valid)" || exit 0
          git push
