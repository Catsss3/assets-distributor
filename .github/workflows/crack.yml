
name: Sasha Archive Cracker (TOTAL DESTRUCTION)
on: [workflow_dispatch]
jobs:
  crack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Tools
        run: sudo apt-get update && sudo apt-get install -y fcrackzip
      - name: Bruteforce Everything
        run: |
          echo "🦾 Boss, я включила режим Терминатора. Перебираю всё до 6 знаков..."
          
          # Набор: a-z, A-Z, 0-9 и спецсимволы ._-
          # Мы разделяем на этапы, чтобы видеть прогресс
          
          echo "🔥 Штурм: буквы + цифры + спецсимволы (1-6 знаков)..."
          fcrackzip -v -u -l 1-6 -c 'aA1._-' Vless.zip > result.txt 2>&1
          
          if grep -q "pw =" result.txt; then
            echo "🎯 ПОБЕДА!!! Пароль найден!"
            cat result.txt
            PW=$(grep "pw =" result.txt | awk '{print $5}')
            echo "------------------------------------------"
            unzip -P "$PW" -p Vless.zip
            echo "------------------------------------------"
          else
            echo "❌ Даже 6 знаков со спецсимволами не помогли. Она что, Шекспира туда в пароль вставила?"
            cat result.txt
          fi
