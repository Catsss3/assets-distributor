
name: Sasha Archive Cracker (Final Assault)
on: [workflow_dispatch]
jobs:
  crack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Tools
        run: sudo apt-get update && sudo apt-get install -y fcrackzip
      - name: Heavy Attack
        run: |
          echo "🕵️‍♀️ Погнали, Boss! Токены на месте, начинаем штурм!"
          
          # Наш секретный список слов
          echo "sasha" > pass.txt
          echo "Sasha" >> pass.txt
          echo "SashaV" >> pass.txt
          echo "vpnclub" >> pass.txt
          echo "2025" >> pass.txt
          echo "vless" >> pass.txt
          echo "v2ray" >> pass.txt
          
          # Пробуем словарь мгновенно
          fcrackzip -v -u -D -p pass.txt Vless.zip && exit 0 || echo "Словарь не подошел."

          # Пробуем буквы + цифры (смесь, до 5 знаков)
          echo "🚀 Проверяем короткие смеси (буквы + цифры)..."
          fcrackzip -v -u -l 1-5 -c 'a1' Vless.zip && exit 0
